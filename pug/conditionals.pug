doctype
html
    head
        title
    body
        div
        - let user = {name:"kuldeep",loginIn:true,lastLogin:6}
            if user.loginIn
                p
                    | welcome back.
                    strong #{ user.name }
            else if user.lastLogin < 10
                p you lasy login was #{user.lastLogin} minute ago
                p
                   a(href="/login") Log in Again 
                    
            else
                a(href="/login") Log in